// let supabase: any

// // Nếu đang chạy trong React Native (Expo)
// if (
//   typeof navigator !== 'undefined' &&
//   navigator.product === 'ReactNative'
// ) {
//   supabase = require('./mobile').supabase
// } else {
//   // Mặc định là Web (Next.js)
//   supabase = require('./client').supabase
// }

// export { supabase }
